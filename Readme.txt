只有在开发时才需要使用自动编译工具，例如webpack-dev-server,webpack-dev-middleware,包括开启watch模式
项目上线时都会直接使用webpack进行打包构建，不需要使用这些自动编译工具
自动编译工具只是为了提高开发体验 官方强烈建议使用webpack-dev-server

抽离的三个配置文件，重新建立一个build文件夹放置，此时会存在一些配置文件中的路径问题，这里需要注意若配置文件中所有用的'./'类型的相对路径，则不会有问题，此时物理目录虽然在build目录下，但是相对路径依然相对于项目跟目录（这里是特殊情况，一般还是遵循当前相对），但是如果配置文件有path模块拼接的绝对路径，则就以build当前为跟目录，所以会存在找不到资源的问题，此时需要手都修改path.join(__dirname, '..', 路径)